name: pr-gate-bootstrap

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: read

jobs:
  bootstrap-guard:
    runs-on: ubuntu-latest
    steps:
      - run: echo "temporary unlock mode"
