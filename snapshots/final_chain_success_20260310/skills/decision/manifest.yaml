skill_name: decision
purpose: 複数案から1つを選び理由を明確にする

inputs:
  - name: options
    type: list[string]

outputs:
  - name: selected_option
    type: string

invoke_when:
  - 複数の選択肢があるとき

avoid_when:
  - 選択肢が1つしかないとき
